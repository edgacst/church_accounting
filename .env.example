# 환경 설정 예시 파일
# 실제 사용 시: 이 파일을 복사해서 .env 파일로 만들고 값을 채우세요

# Django 비밀키 (python manage.py shell -c "from django.core.management.utils import get_random_secret_key; print(get_random_secret_key())" 로 생성)
SECRET_KEY=여기에_새로_생성한_비밀키를_입력하세요

# 디버그 모드 (개발: True, 실제 운영: False)
# 중요: 실제 서비스할 때는 반드시 False로 설정하세요!
DEBUG=True

# 허용할 도메인 (쉼표로 구분, 예: localhost,127.0.0.1,church.example.com)
# 실제 운영: 실제 도메인 주소로 변경하세요
ALLOWED_HOSTS=localhost,127.0.0.1
